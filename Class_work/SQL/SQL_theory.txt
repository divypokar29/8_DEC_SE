what is database ?
---------------------------------------------

Database is a collection of data.

which is contain data in tabiular formate

Row and colume wise we can store multiple  data in simple frame.

Row reperesent single record and colume reperesent specific field name of that record 

e.g


name     subject     city     pincode     (colume name)
---------------------------------------------------------
rakesh    MCA       indore    452015       (row)



we can store multiple  records we can retrive records specific condition wise 
even we caan updete and delete records condtion wise.
------------------------------------------------------------------------------
=> DDL Data defination language:

 to create structure of database and table we used DDl commands using below
 commands we can create structure we can add remove fields we can remove  table 
 or we can renmae it using DDL commands .

  1) create
  2) Alter 
  3) drop
  4) truncate
  5) select

 --------------------------------------------------------------
 => DQL = data Query language

  1) select


  ---------------------------------------------------------

  => DCL : data control language

  1) grant
  2) remove

  ------------------------------------------------------------

  => TCL = Trangaction control language 

  1) commit
  2) rollback
  3) savepoint  
----------------------------------------------------------------------------
SQL  : structure Query language 

it is a programing language 

using SQL we can perform all database operations.

1.create 
2.read
3.updete
4.delete

--------------------------------------------------------------

login with mysql cmd panel

#mysql -u root -panel
Enter your password :

----------------------------------------
Create database syntax :
   
   create database <databasename>;

   e.g

   create database 8DECSE;

   now, next step to check all databses;


     # show databaases;

   now ,next remove databse 

     # drop database <databasename>

     e.g

      # drop database mydb;
      show databse;

--------------------------------------------------------------------------------------
now , next select your database which you want to workupon it 

syntax :
   -> use <databasename>;

   e.g.

   # use 8decse;

------------------------------------------------------------------------------

table: one database can contain as many as tables

 e.g
   
   table contain row and colume 

next step: table creation.

 
  syntax :
    
     create table<tablename> (
        <columename>  <datatype>
        <columename>  <datatype>
        <columename>  <datatype>
     );

e.g
  
   create table Student 
   (
    id int,
    name varchar(20),
    subject varchar(20),
    score int
   );

now, next check table is proper created or not using dascribe command 

syntax :  #desc <tablename>;

e.g

   -> desc student;

-------------------------------------------------------

Alter : alter command is most important part of DDL commands

   using of alter command we can add , remove or modifiy field of table 

   1) add
   2) drop
   3) modifiy

 

    syntax : 
       
       alter table <tablename> <altercommand> fieldname datatype


     e.g 

        -> alter table student add city varchar(20);

        -> desc student;
--------------------------------------
    syntax: 
      
      alter table student <drop> fieldname;

    e.g 
      
       altet table student add dummy varchar(20);

       alter table student drop dummy;
----------------------------------------------

modifiy : when we want to channge any field data type or field size we can 
use modifiy

syntax : 
         
         alter table <tablename> modifiy <columename> <datatype>;

         e.g.

         alter table student modifiy name varchar(25);

         ->show databaases;
         ->use databas;
         ->show table;
         ->desc student;
         ->alter table student modify city varchar(30);
         ->desc student;
--------------------------------------------------------

=> alter rename command 

 
      when we want to channge colume name using of rename command

      syntax:
         
         alter table <tablename> rename colume <columename> to <newcolume>;

         e.g

          -> alter table student rename colume score to marks;
          -> desc student;

----------------------------------------------------------------
3) drop : to delete table or daatabase from the memory we can use drop command

   ->syntax:
       
        drop table <tablename>;

        or

        drop databas <databasename>;

        e.g.
         
         -> create table dummy
         (
            id int,
            name varchar(20),
            sunada varchar(20)
         );
    
    -> show tables;
    -> drop table dummy;
    ->show tables;

----------------------------------------------------------------------

4) Truncate : 

      drop delete table and colum from the memory completellty but 
      using of truncate command we can remove all records from the table 
      but structure remain same 

      means using of truncate we dont need to recreateate table but using of 
      drop command we have to create table first re enter data.

      truncate is faster then drop becuse it delete all record from the table,

      e.g/
      drop : account delete
      truncate : delte order history or serch history.......


      syntax: 

      # truncate table <tablename>;

       create table dummy(
         id int,
         name varchar(20)
       );
     
     truncate table dummy;

     ahow tables;

     ----------------------------------------------------------------------------------

     5) Rename : when we want to channge table name we can use rename command 
                 
            syntax: 
                rename table <oldname> to <newname>;

                show table ;

                -> note : we can also rename tablle name using of alter command 

                syntax :
                 
                 alter table <oldname> rename to <newname>;
====================================================================================
DML : Data manipulation language :

   
   1) insert 
   2) update
   3) delete 

   1) insert : insert command is used to score record in existing table using of specific 
               colume wise.


               syntax:
                  
                  insert inti <tablename>
                              (coloumname1,columename2......)
                        values
                             (value1,value2.................);

               e.g.
                  
                   insert into student
                   (id,name,subjec,score,city)
                   values
                   (1,'divy','hindi',52,'ahwmdan');

          ** =>DQL : selecct:

                  to retrive all record from table 

                  syntax : 
                     
                     select * from <tablename>;

                     e.g.

                      select * from student;
+------+------+--------+-------+---------+
| id   | name | subjec | score | city    |
+------+------+--------+-------+---------+
|    1 | divy | hindi  |    52 | ahwmdan |
+------+------+--------+-------+---------+
--------------------------------------------------------------------------------------

store multiple record usinng single insert command 

=> insert into student
  (id,name,subjec,score,city),
  values
  (2,'riya','java',95,'surat'),
  (3,'diya','slutter',46,'kutch');


=> without adding and columname in command 

    -> insert into student 
    values(4,'kisan','python',15,'sfsf');


--------------------------------------------------------------------------------------

      Constraints :
       
       1) NOT NULL
       2) DEFAULT
       3) PRIMARY KEY
       4) UNIQUE
       5) AUTO INCRIMATE
       6) check
       7) FORIGN KEY












       6) CHECK : CHECK IS A ConstrainT which is mainly used to apply restriction on specific field
                  for e.g emp table we can allow only more than 18 year old emply 

                  syntax : 

                      fieldname check fieldname condition;

                  =>create table user
                     (
                     id int primary key auto_increment,
                     name  varchar(20),
                     age int check (age > 18)
                     );

                      desc user;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| name  | varchar(20) | YES  |     | NULL    |                |
| age   | int         | YES  |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+


                  => insert into user (id,name,age)
                     value (1,'raj',20);

                  => selecct *  from user;

                  =>insert into user (id,name,age) 
                    values(2,'hol',4);
                    ERROR 3819 : check Constraint 'user_chk_1' is violated.


                    => alter table user drop column age;

                    => alter table user add colum age int;

                    => alter table user add Constraint check_age check (age>18);
                    add UNIQUE constrain name using of alter command

                    => alter table user add constrain user_pk primary key(id);

                    =>alter table user drop primary key;

======================================================================================

2) updete : when we want to update records based on specific condition wise we can use update command
            
            update command is a part od DML (data manipulation laanguage)

            syntax : 


            update <tablename> set columnname=fieldname,columename=fieldname
            where condition;
            

            ->select * from student;
            ->update student set marks=55 where id =2;
            ->select * from student;

             select * from student ;
+------+-------+--------+-------+---------+
| id   | name  | subjec | score | city    |
+------+-------+--------+-------+---------+
|    1 | divy  | hindi  |    52 | ahwmdan |
|    2 | roiya | java   |    55 | surat   |
+------+-------+--------+-------+---------+


 -> update student set name='jan',subject='java',city-'jamnagar' where id =1;

 select * from student;
+------+------+--------+-------+----------+
| id   | name | subjec | score | city     |
+------+------+--------+-------+----------+
|    1 | divy | hindi  |    52 | ahwmdan  |
|    2 | jan  | java   |    55 | jamnagar |
+------+------+--------+-------+----------+

->update student set name='riya' where name='divy';

select * from student;
+------+------+--------+-------+----------+
| id   | name | subjec | score | city     |
+------+------+--------+-------+----------+
|    1 | Riya | hindi  |    52 | ahwmdan  |
|    2 | jan  | java   |    55 | jamnagar |
+------+------+--------+-------+----------+

====================================================================================================
3) Delete : when we want to delete or rename specific row condition wise we can 
           use delete command

           syntax:

            -> delete from <tablename>  where condition;
            ->delete from student where id=1;
            select * from student;
+------+-------+---------+-------+-----------+
| id   | name  | subjec  | score | city      |
+------+-------+---------+-------+-----------+
|    2 | jan   | java    |    55 | jamnagar  |
| NULL | rana  | bhanu   |    85 | bhopal    |
| NULL | jugnu | wnglis  |    15 | rajisthan |
| NULL | rahul | gujrati |    32 | mumbai    |
+------+-------+---------+-------+-----------+
4 rows in set (0.00 sec)

-> delete from studetn where score =>20;

------------------------------------------------------------------------------------------------------

III) DQL : Data Query language:

      most most powerfull concept of sql 

      when we want to retrive records from the table we can use DQL command

      -> select

using or select command we can retrive difrent diffrent record with specific condition wise.


syntax:

-> select * from <tablename>;

e.g. ->select * from student ;

fetch specific colume from table 


syntax :
    
     -> select fieldname,fieldname2,............... from <tablename>;

     e.g.  select name,subject from student;

     fetch specific data using of condition wise
     for condition we use where clause.

     syntax: select * from <tablename> where condition;

     e.g. -> select * from student where city='ahemdabade';

     -> select name ,subjec,city.from student where city = 'ahemdabade';


     conditional operaters : 

               => , != ,> ,< ,>= ,<=

     logical operators : 

               => and, or, not
      
      membership operators :

               => in , not in , between

      pattern matching operators :
              
               => like, not like

      null operators :

               => is null , is  not null

-------------------------------------------------------

-> select * from student where marks < 60;

+------+-------+---------+-------+-----------+
| id   | name  | subjec  | score | city      |
+------+-------+---------+-------+-----------+
|    2 | jan   | java    |    55 | jamnagar  |
| NULL | jugnu | wnglis  |    15 | rajisthan |
| NULL | rahul | gujrati |    32 | mumbai    |
+------+-------+---------+-------+-----------+

-> select * from student where score > 50  and score <60;
-> select student set score = 40,city='mumbai' where id =1;
-> select * from student;
-> select * from student where subject ='java', and city='surat';
-> select from student where subjec = 'java' or subject='python';
->select name,subjec from studetn where subjec != 'java';

->selecct name,subjec,city from student where subject in('java','python');
->select name,city from student where city in ('ahemdabade','surat');
-> selecct name,city from student where city not in ('ahemdabade','surat');
->select  name,score from student where score between 50 and 60;

-----------------------------------------------------------------------------------

=>LIKE:

 syntax: select * from <tablename> where name like '%a%';

 ->select * from student where name like '%a%';

 ->select * from student where name like 'a%';

 ->select * from student where name like '%a';
 
 ->select * from student where name like '_a%';
 
 ->select * from student where name like '%a_';
 
 ->select * from student where name like '____a%';

 ------------------------------------------------------------------------------

 => IS NULL / NOT NULL 

    to check any field contain null value or not using of IS NULL and IS NOT NULL  condition
    syntax : 

      select fieldname from <tablename> where columnname is not null;

      -> select * from student where name is not null;
      -> select * from student where name is null;

---------------------------------------------------------------------------

order by : when we want to retrive data from the database with specific oederformate

         by Default order formate is ascending order 

         there are 2 keyword ASC and desc
         shyntax:

         select * from <tablename> oeder by <columnname>;
         or
         select * from <tablename> oeder by <columnname> asc;
         or
         select * from <tablename> oeder by <columnname> desc;

         => e.g.

         -> select * from student order by name;
         -> select * from student order by score asc;
         -> select * from student order by score desc;
         -> select * from studetn order by score desc limit 3;
         -> select * from student order by name,subjec;

-------------------------------------------------------------------------------

GROUP BY : group by row wise contain common colum value 

to use concept of group by we have to use aggreagate functions

->count(*) : count no of records
->sum : total
->max : maximaum value
->min : minimum value
->avg : average value


syntax: 

    selecct columname ,count (*) from <tablename> group by <columnname>;

   e.g.
   select city  ,count (*) from student group by city;

+-----------+----------+
| city      | count(*) |
+-----------+----------+
| jamnagar  |        1 |
| bhopal    |        1 |
| rajisthan |        1 |
| mumbai    |        1 |
+-----------+----------+


->select city ,sum(score) from student group by city;
+-----------+------------+
| city      | sum(score) |
+-----------+------------+
| jamnagar  |         55 |
| bhopal    |         85 |
| rajisthan |         15 |
| mumbai    |         32 |
+-----------+------------+

-> select city ,max(score) from student group by city;
+-----------+------------+
| city      | max(score) |
+-----------+------------+
| jamnagar  |         55 |
| bhopal    |         85 |
| rajisthan |         15 |
| mumbai    |         32 |
+-----------+------------+

->select city ,min(score) from student group by city;
+-----------+------------+
| city      | min(score) |
+-----------+------------+
| jamnagar  |         55 |
| bhopal    |         85 |
| rajisthan |         15 |
| mumbai    |         32 |
+-----------+------------+

-> select city ,avg(score) from student group by city;
+-----------+------------+
| city      | avg(score) |
+-----------+------------+
| jamnagar  |    55.0000 |
| bhopal    |    85.0000 |
| rajisthan |    15.0000 |
| mumbai    |    32.0000 |
+-----------+------------+

=> create table product(
    -> id int primary key auto_increment,
    -> product_name varchar(20),
    -> product_qty int,
    -> product_price int
    -> );

=> insert into product(product_name,product_qty,product_price)
    -> values('tv',30,80000),
    -> ('ac',120,120000),
    -> ('mobile',70,10000),
    -> ('laptop',40,80000),
    -> ('ac',20,100000),
    -> ('tv',80,190000);

-> select * from product;
+----+--------------+-------------+---------------+
| id | product_name | product_qty | product_price |
+----+--------------+-------------+---------------+
|  1 | tv           |          30 |         80000 |
|  2 | ac           |         120 |        120000 |
|  3 | mobile       |          70 |         10000 |
|  4 | laptop       |          40 |         80000 |
|  5 | ac           |          20 |        100000 |
|  6 | tv           |          80 |        190000 |
+----+--------------+-------------+---------------+ 

-> select product_name , count(*) from product group by product_name;
+--------------+----------+
| product_name | count(*) |
+--------------+----------+
| tv           |        2 |
| ac           |        2 |
| mobile       |        1 |
| laptop       |        1 |
+--------------+----------+

----------------------------------------------------------------

==> where and having clause

 where  : apply condition before group

 having : apply condition after group 

 syntax : 
     
        select fieldname ,count(*) from <tablename> group by <fieldname>
            having fieldname condition;

      ->select * from product;

      ->select product_name,count(*) from product group by product_name;

      ==> select product_name,count(*) as Total_Product from product group by product_name;

+--------------+---------------+
| product_name | Total_Product |
+--------------+---------------+
| tv           |             2 |
| ac           |             2 |
| mobile       |             1 |
| laptop       |             1 |
+--------------+---------------+    


==> select product_name as Product,count(*) as Total_Product from product group by product_name having Total_Product >= 2;
+---------+---------------+
| Product | Total_Product |
+---------+---------------+
| tv      |             2 |
| ac      |             2 |
+---------+---------------+

==>select product_name as Product ,sum(product_price) as Price from product group by product_name having Price >= 100000;

+---------+--------+
| Product | Price  |
+---------+--------+
| tv      | 270000 |
| ac      | 220000 |
+---------+--------+

==========================================================================


=> select product_name ,sum(product_price) as total from product where product_qty > 30 group by product_name;
+--------------+--------+
| product_name | total  |
+--------------+--------+
| ac           | 120000 |
| mobile       |  10000 |
| laptop       |  80000 |
| tv           | 190000 |
+--------------+--------+

=> select product_name ,sum(product_price) as total from product where product_qty > 30 group by product_name 
   having sum(product_price) > 50000;
+--------------+--------+
| product_name | total  |
+--------------+--------+
| ac           | 120000 |
| laptop       |  80000 |
| tv           | 190000 |
+--------------+--------+


=>select distinct product_name from product;
+--------------+
| product_name |
+--------------+
| tv           |
| ac           |
| mobile       |
| laptop       |
+--------------+

-----------------------------------------------------------------------------------------------------

JOIN : when we want to join 2 or more tables with using os same mathing value its called joining 

      there is no need to apply FORIGN key for joining 


   =>create table city(
      id int primary key auto_increment,
      name varchar(20)
      );

   =>insert into city (name)
      values
      ("ahemdabade"),
      ("surat "),
      ("mumbai"),
      ("bhopal"),
      ("pune");

   => select * from city;
+----+------------+
| id | name       |
+----+------------+
|  1 | ahemdabade |
|  2 | surat      |
|  3 | mumbai     |
|  4 | bhopal     |
|  5 | pune       |
+----+------------+

   =>  create table user(
        id int primary key auto_increment,
        u_name varchar(20),
        city_id int
      );

   => insert into user(
       u_name,city_id)
       values
       ("ram",1),
       ("syam",3),
       ("rahul",4),
       ("raj",3),
       ("raju",5);

   => select * from user;
+----+--------+---------+
| id | u_name | city_id |
+----+--------+---------+
|  1 | ram    |       1 |
|  2 | syam   |       3 |
|  3 | rahul  |       4 |
|  4 | raj    |       3 |
|  5 | raju   |       5 |
+----+--------+---------+
===============================================================================================
===>select user.u_name as username,city.name from user join city on user.city_id = city.id;
+----------+------------+
| username | name       |
+----------+------------+
| ram      | ahemdabade |
| syam     | mumbai     |
| rahul    | bhopal     |
| raj      | mumbai     |
| raju     | pune       |
+----------+------------+
===============================================================================================

Real word example of 3 table joining 


=> create table tbl_order(
      order_id int primary key auto_increment,
      cust_id int,
      pro_id int);

=>insert into tbl_order(cust_id,pro_id)
      values
      (1,1),
      (1,2),
      (2,4),
      (4,5);


=> select * from tbl_order;
+----------+---------+--------+
| order_id | cust_id | pro_id |
+----------+---------+--------+
|        1 |       1 |      1 |
|        2 |       1 |      2 |
|        3 |       2 |      4 |
|        4 |       4 |      5 |
+----------+---------+--------+
=======================================================
=> Expected Output :

Order_id  CustomerName ProductName product_price

========================================================

=>select tbl_order.order_id,user.u_name,product.product_price
       from tbl_order
       join user
       on tbl_order.cust_id = user.id
      
       join product
       on tbl_order.pro_id = product.id;
+----------+--------+---------------+
| order_id | u_name | product_price |
+----------+--------+---------------+
|        1 | ram    |         80000 |
|        2 | ram    |        120000 |
|        3 | syam   |         80000 |
|        4 | raj    |        100000 |
+----------+--------+---------------+

----------------------------------------------------------------------------

1) INNER JOIN : inner join which is return only mathing elements from the both tables


insert into tbl_order (cust_id,pro_id)
values
(7,2),
(6,8);


=> select * from tbl_order;
+----------+---------+--------+
| order_id | cust_id | pro_id |
+----------+---------+--------+
|        1 |       1 |      1 |
|        2 |       1 |      2 |
|        3 |       2 |      4 |
|        4 |       4 |      5 |
|        5 |       7 |      2 |
|        6 |       6 |      8 |
+----------+---------+--------+

=> select * from user;
+----+--------+---------+
| id | u_name | city_id |
+----+--------+---------+
|  1 | ram    |       1 |
|  2 | syam   |       3 |
|  3 | rahul  |       4 |
|  4 | raj    |       3 |
|  5 | raju   |       5 |
+----+--------+---------+

=> select user.u_name,tbl_order.pro_id
   from user
   inner join tbl_order
   on user.id = tbl_order.cust_id;

+--------+--------+
| u_name | pro_id |
+--------+--------+
| ram    |      1 |
| ram    |      2 |
| syam   |      4 |
| raj    |      5 |
+--------+--------+

2) Left JOIN : left join take all records from the left table and display mathing
               values from the right table 


               if there is no records in right table it display NULL


select user.u_name,tbl_order.pro_id
      from user 
      left join tbl_order 
      on user.id = tbl_order.cust_id;
+--------+--------+
| u_name | pro_id |
+--------+--------+
| ram    |      1 |
| ram    |      2 |
| syam   |      4 |
| rahul  |   NULL |
| raj    |      5 |
| raju   |   NULL |
+--------+--------+

3) Right join : fetch all records from right table and try to match with left table 

   select * from user
   right join tbl_order
   on user.id = tbl_order.cust_id;

+------+--------+---------+----------+---------+--------+
| id   | u_name | city_id | order_id | cust_id | pro_id |
+------+--------+---------+----------+---------+--------+
|    1 | ram    |       1 |        1 |       1 |      1 |
|    1 | ram    |       1 |        2 |       1 |      2 |
|    2 | syam   |       3 |        3 |       2 |      4 |
|    4 | raj    |       3 |        4 |       4 |      5 |
| NULL | NULL   |    NULL |        5 |       7 |      2 |
| NULL | NULL   |    NULL |        6 |       6 |      8 |
+------+--------+---------+----------+---------+--------+

4) Outer JOIN : fetch all records which is not match 

5) FULL JOIN : fetch all matching record from left side and right side 

   in simple language WE CAN say that full join 
   combination of left and right join
   in mysql full join is not working 

   solution we can combine left join and right join result 

   using of UNION concept 

=========================================================================

UNION : to combine two  tablees without duplicate values we cam use UNION 

create table A 
(
   name varchar(20)
);

insert into A
    values
    ("ajay"),
    ("riya"),
    ("diya"),
    ("raj");

 select * from A;
+------+
| name |
+------+
| ajay |
| riya |
| diya |
| raj  |
+------+

create table B
(
   name varchar(20)
);

insert into B
    values
    ("ajay"),
    ("riya"),
    ("diya"),
    ("raj");

select * from B;
+------+
| name |
+------+
| ajay |
| riya |
| diya |
| raj  |
+------+

select * from A
union
select * from B;
+------+  
| name |
+------+  
| ajay |
| diya |
| raj  |
| riya |
+------+  

select * from A
union all
select * from B;
+------+  
| name |
+------+  
| ajay |
| riya |
| diya |
| raj  |
| ajay |
| riya |
| diya |
| raj  |
+------+  